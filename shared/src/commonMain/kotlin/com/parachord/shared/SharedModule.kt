package com.parachord.shared

/**
 * Parachord shared module — KMP business logic shared between Android and iOS.
 *
 * Phase 0: Empty module, proving the KMP Gradle configuration works.
 * Phase 1+: Models, API clients, repositories, AI services, resolvers,
 *           plugin system, scrobblers migrate here incrementally.
 */
object SharedModule {
    const val VERSION = "0.1.0"
}
