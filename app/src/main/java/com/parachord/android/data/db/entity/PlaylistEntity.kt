package com.parachord.android.data.db.entity

import com.parachord.shared.model.Playlist

/** Typealias so existing code referencing PlaylistEntity continues to compile. */
typealias PlaylistEntity = Playlist
