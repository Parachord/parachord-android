package com.parachord.android.data.db.entity

import com.parachord.shared.model.ChatMessageRecord

/** Typealias so existing code referencing ChatMessageEntity continues to compile. */
typealias ChatMessageEntity = ChatMessageRecord
