package com.parachord.android.data.db.entity

import com.parachord.shared.model.Friend

/** Typealias so existing code referencing FriendEntity continues to compile. */
typealias FriendEntity = Friend
